# fedora-debloat

Remove packages that are useless in virtualized environment.

```fedora-bloat.txt``` contains list of packages that are up to be removed.

```fedora-debloat.yml``` is an Ansible playbook to debloat en masse.
